import { useRef, useState } from "react";
import { Canvas, extend, useFrame } from "@react-three/fiber";
import { Html } from "@react-three/drei";

export default function About(props) {
  return <group position={[0, 0.5, 5]} scale={0.5}></group>;
}
